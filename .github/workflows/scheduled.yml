name: scheduled

on:
  schedule:
    - cron: "14 */1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scheduled
  cancel-in-progress: false

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: refresh
        run: |
          set -eu
          sleep $(( RANDOM % 900 ))
          f="README.md"
          [ -f "$f" ] || exit 0
          stamp="<!-- checked: $(date -u +%Y-%m-%d) -->"
          if grep -q "^<!-- checked: " "$f"; then
            sed -i "s|^<!-- checked: .*|$stamp|" "$f"
          else
            printf '\n%s\n' "$stamp" >> "$f"
          fi

      - name: commit
        run: |
          set -eu
          git config user.name "ryanv-dev2095l1"
          git config user.email "279199413+ryanv-dev2095l1@users.noreply.github.com"
          set -- "docs: bump date" "docs: refresh timestamp" "docs: keep current" "chore: refresh metadata" "chore: periodic update"
          n=$(( ${GITHUB_RUN_NUMBER:-0} % $# + 1 ))
          eval msg=\${$n}
          git add -- "README.md"
          if git diff --cached --quiet; then
            echo "nothing to commit"
            exit 0
          fi
          git commit -m "$msg"
          git push
